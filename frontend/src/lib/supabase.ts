/**
 * Supabase client stub — no longer used for auth.
 * Auth is now handled entirely by the backend via JWT tokens.
 * This file exists only for backward compatibility with any remaining imports.
 */

// No-op export — auth is handled by backend
export const supabase = null;
